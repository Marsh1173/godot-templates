Controlled camera is a simple node, with a ray originating at the position of
the node and pointing backward until it collides with something in layer 1
(terrain). At the point of the ray, the camera exists, with some motion
smoothing.
